# Copy to .env — zero-cost testnet only, never commit real keys
VITE_REGISTRY=
VITE_POOL=
VITE_VERIFIER=
# Optional: custom Sepolia RPC (default publicnode is fine, no key needed)
VITE_SEPOLIA_RPC=https://ethereum-sepolia-rpc.publicnode.com
